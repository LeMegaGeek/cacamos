diff --git a/kernel/xiaomi/sdm845/arch/arm64/configs/vendor/xiaomi/dipper.config b/kernel/xiaomi/sdm845/arch/arm64/configs/vendor/xiaomi/dipper.config
index 1031c561a8d6301256ed91de38578fd84125307c..213e93fe90d5e2fb148a0750a49a1ddba4ef0a44 100644
--- a/kernel/xiaomi/sdm845/arch/arm64/configs/vendor/xiaomi/dipper.config
+++ b/kernel/xiaomi/sdm845/arch/arm64/configs/vendor/xiaomi/dipper.config
@@ -12,5 +12,8 @@ CONFIG_TOUCHSCREEN_ST_FTS_V521=y
 CONFIG_USE_ROHM_BU64753=y
 CONFIG_FINGERPRINT_FPC_SCREEN_NOTIFY=y
 CONFIG_I2C_BY_DMA=y
+CONFIG_MEDIA_USB_SUPPORT=y
+CONFIG_USB_VIDEO_CLASS=y
+CONFIG_USB_CONFIGFS_F_UVC=y
 # CONFIG_TOUCHSCREEN_ATMEL_MXT_T37 is not set
 # CONFIG_TOUCHSCREEN_ST_DEBUG_FS is not set
diff --git a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.c b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.c
index 650bcafa7e2a3ce7840c4cc4f183189ff53245d9..1831bb49822385953a182d65e8ad53e14478c9be 100644
--- a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.c
+++ b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.c
@@ -262,10 +262,13 @@ uvc_function_setup(struct usb_function *f, const struct usb_ctrlrequest *ctrl)
 	return 0;
 }
 
-void uvc_function_setup_continue(struct uvc_device *uvc)
+void uvc_function_setup_continue(struct uvc_device *uvc, int disable_ep)
 {
 	struct usb_composite_dev *cdev = uvc->func.config->cdev;
 
+	if (disable_ep && uvc->video.ep)
+		usb_ep_disable(uvc->video.ep);
+
 	usb_composite_setup_continue(cdev);
 }
 
@@ -333,15 +336,11 @@ uvc_function_set_alt(struct usb_function *f, unsigned interface, unsigned alt)
 		if (uvc->state != UVC_STATE_STREAMING)
 			return 0;
 
-		if (uvc->video.ep)
-			usb_ep_disable(uvc->video.ep);
-
 		memset(&v4l2_event, 0, sizeof(v4l2_event));
 		v4l2_event.type = UVC_EVENT_STREAMOFF;
 		v4l2_event_queue(&uvc->vdev, &v4l2_event);
 
-		uvc->state = UVC_STATE_CONNECTED;
-		return 0;
+		return USB_GADGET_DELAYED_STATUS;
 
 	case 1:
 		if (uvc->state != UVC_STATE_CONNECTED)
diff --git a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.h b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.h
index d0a73bdcbba105acb6a2bb11322381ba00dc2f43..b74c6e74e9b11b96d79b3cb3493d0aabed0d315b 100644
--- a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.h
+++ b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/f_uvc.h
@@ -18,7 +18,7 @@
 
 #include "uvc.h"
 
-void uvc_function_setup_continue(struct uvc_device *uvc);
+void uvc_function_setup_continue(struct uvc_device *uvc, int disable_ep);
 
 void uvc_function_connect(struct uvc_device *uvc);
 
diff --git a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc.h b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc.h
index d1649f8997db1ffd5f2628dded894fa488a45b0e..2c546538868d2aa735699168d7871177427951ae 100644
--- a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc.h
+++ b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc.h
@@ -117,11 +117,13 @@ struct uvc_video
 	struct mutex mutex;	/* protects frame parameters */
 
 	/* Requests */
+	bool is_enabled;
 	unsigned int req_size;
 	struct usb_request *req[UVC_NUM_REQUESTS];
 	__u8 *req_buffer[UVC_NUM_REQUESTS];
 	struct list_head req_free;
 	spinlock_t req_lock;
+	wait_queue_head_t req_wait;
 
 	void (*encode) (struct usb_request *req, struct uvc_video *video,
 			struct uvc_buffer *buf);
@@ -188,7 +190,7 @@ struct uvc_file_handle
  * Functions
  */
 
-extern void uvc_function_setup_continue(struct uvc_device *uvc);
+extern void uvc_function_setup_continue(struct uvc_device *uvc, int disable_ep);
 extern void uvc_endpoint_stream(struct uvc_device *dev);
 
 extern void uvc_function_connect(struct uvc_device *uvc);
diff --git a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_v4l2.c b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_v4l2.c
index f4ccbd56f4d254c42112f3ae89949aefe6d5c8d1..f978cd9a28e081f22d9307e27897a7afe4c933e4 100644
--- a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_v4l2.c
+++ b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_v4l2.c
@@ -210,8 +210,8 @@ uvc_v4l2_streamon(struct file *file, void *fh, enum v4l2_buf_type type)
 	 * Complete the alternate setting selection setup phase now that
 	 * userspace is ready to provide video frames.
 	 */
-	uvc_function_setup_continue(uvc);
 	uvc->state = UVC_STATE_STREAMING;
+	uvc_function_setup_continue(uvc, 0);
 
 	return 0;
 }
@@ -222,11 +222,29 @@ uvc_v4l2_streamoff(struct file *file, void *fh, enum v4l2_buf_type type)
 	struct video_device *vdev = video_devdata(file);
 	struct uvc_device *uvc = video_get_drvdata(vdev);
 	struct uvc_video *video = &uvc->video;
+	bool complete_delayed_status;
+	int ret;
 
 	if (type != video->queue.queue.type)
 		return -EINVAL;
 
-	return uvcg_video_enable(video, 0);
+	complete_delayed_status = uvc->state == UVC_STATE_STREAMING;
+
+	/*
+	 * Stop USB completions before releasing their backing requests.  The
+	 * endpoint disable also gives back every request synchronously on DWC3.
+	 */
+	if (video->ep && video->ep->enabled)
+		usb_ep_disable(video->ep);
+
+	ret = uvcg_video_enable(video, 0);
+
+	if (complete_delayed_status) {
+		uvc->state = UVC_STATE_CONNECTED;
+		uvc_function_setup_continue(uvc, 0);
+	}
+
+	return ret;
 }
 
 static int
diff --git a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_video.c b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_video.c
index 91fd7821248804b3662f2f2ae9168e3304a243c4..7254b61e795373d89b523a0aae86cda3d8b58825 100644
--- a/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_video.c
+++ b/kernel/xiaomi/sdm845/drivers/usb/gadget/function/uvc_video.c
@@ -208,7 +208,15 @@ uvc_video_complete(struct usb_ep *ep, struct usb_request *req)
 
 	video->encode(req, video, buf);
 
+	spin_lock(&video->req_lock);
+	if (!video->is_enabled || !video->ep->enabled) {
+		spin_unlock(&video->req_lock);
+		spin_unlock_irqrestore(&video->queue.irqlock, flags);
+		goto requeue;
+	}
+
 	ret = uvcg_video_ep_queue(video, req);
+	spin_unlock(&video->req_lock);
 	spin_unlock_irqrestore(&video->queue.irqlock, flags);
 
 	if (ret < 0) {
@@ -222,6 +230,7 @@ uvc_video_complete(struct usb_ep *ep, struct usb_request *req)
 	spin_lock_irqsave(&video->req_lock, flags);
 	list_add_tail(&req->list, &video->req_free);
 	spin_unlock_irqrestore(&video->req_lock, flags);
+	wake_up_all(&video->req_wait);
 }
 
 static int
@@ -316,7 +325,8 @@ int uvcg_video_pump(struct uvc_video *video)
 		 * request lock.
 		 */
 		spin_lock_irqsave(&video->req_lock, flags);
-		if (list_empty(&video->req_free)) {
+		if (!video->is_enabled || !video->ep->enabled ||
+		    list_empty(&video->req_free)) {
 			spin_unlock_irqrestore(&video->req_lock, flags);
 			return 0;
 		}
@@ -337,8 +347,16 @@ int uvcg_video_pump(struct uvc_video *video)
 
 		video->encode(req, video, buf);
 
+		spin_lock(&video->req_lock);
+		if (!video->is_enabled || !video->ep->enabled) {
+			spin_unlock(&video->req_lock);
+			spin_unlock_irqrestore(&queue->irqlock, flags);
+			break;
+		}
+
 		/* Queue the USB request */
 		ret = uvcg_video_ep_queue(video, req);
+		spin_unlock(&video->req_lock);
 		spin_unlock_irqrestore(&queue->irqlock, flags);
 
 		if (ret < 0) {
@@ -350,9 +368,30 @@ int uvcg_video_pump(struct uvc_video *video)
 	spin_lock_irqsave(&video->req_lock, flags);
 	list_add_tail(&req->list, &video->req_free);
 	spin_unlock_irqrestore(&video->req_lock, flags);
+	wake_up_all(&video->req_wait);
 	return 0;
 }
 
+static bool uvc_video_all_requests_free(struct uvc_video *video)
+{
+	struct usb_request *req;
+	unsigned int allocated = 0;
+	unsigned int free = 0;
+	unsigned int i;
+	unsigned long flags;
+
+	for (i = 0; i < UVC_NUM_REQUESTS; ++i)
+		if (video->req[i])
+			++allocated;
+
+	spin_lock_irqsave(&video->req_lock, flags);
+	list_for_each_entry(req, &video->req_free, list)
+		++free;
+	spin_unlock_irqrestore(&video->req_lock, flags);
+
+	return free == allocated;
+}
+
 /*
  * Enable or disable the video stream.
  */
@@ -360,6 +399,7 @@ int uvcg_video_enable(struct uvc_video *video, int enable)
 {
 	unsigned int i;
 	int ret;
+	unsigned long flags;
 
 	if (video->ep == NULL) {
 		printk(KERN_INFO "Video enable failed, device is "
@@ -368,10 +408,22 @@ int uvcg_video_enable(struct uvc_video *video, int enable)
 	}
 
 	if (!enable) {
+		spin_lock_irqsave(&video->req_lock, flags);
+		video->is_enabled = false;
+		spin_unlock_irqrestore(&video->req_lock, flags);
+
 		for (i = 0; i < UVC_NUM_REQUESTS; ++i)
 			if (video->req[i])
 				usb_ep_dequeue(video->ep, video->req[i]);
 
+		if (!wait_event_timeout(video->req_wait,
+					uvc_video_all_requests_free(video),
+					msecs_to_jiffies(1000))) {
+			pr_err("%s: timed out draining UVC requests\n",
+			       __func__);
+			return -ETIMEDOUT;
+		}
+
 		uvc_video_free_requests(video);
 		uvcg_queue_enable(&video->queue, 0);
 		return 0;
@@ -383,6 +435,10 @@ int uvcg_video_enable(struct uvc_video *video, int enable)
 	if ((ret = uvc_video_alloc_requests(video)) < 0)
 		return ret;
 
+	spin_lock_irqsave(&video->req_lock, flags);
+	video->is_enabled = true;
+	spin_unlock_irqrestore(&video->req_lock, flags);
+
 	if (video->max_payload_size) {
 		video->encode = uvc_video_encode_bulk;
 		video->payload_size = 0;
@@ -399,6 +455,8 @@ int uvcg_video_init(struct uvc_video *video)
 {
 	INIT_LIST_HEAD(&video->req_free);
 	spin_lock_init(&video->req_lock);
+	init_waitqueue_head(&video->req_wait);
+	video->is_enabled = false;
 
 	video->fcc = V4L2_PIX_FMT_YUYV;
 	video->bpp = 16;

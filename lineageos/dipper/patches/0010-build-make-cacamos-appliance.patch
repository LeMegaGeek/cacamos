diff --git a/build/make/target/product/base_system.mk b/build/make/target/product/base_system.mk
index d4911b62ff..8805007a3d 100644
--- a/build/make/target/product/base_system.mk
+++ b/build/make/target/product/base_system.mk
@@ -350,7 +350,14 @@ ifneq ($(PRODUCT_NO_DYNAMIC_SYSTEM_UPDATE),true)
 
 endif
 
-# Check if the build supports NFC apex or not
+# The dedicated appliance keeps Android's public NFC API jar but omits the NFC
+# service module. Starting com.android.nfc without the device HAL causes a
+# permanent ANR restart loop.
+ifeq ($(CACAMOS_APPLIANCE),true)
+    PRODUCT_PACKAGES += \
+        framework-nfc
+endif
+ifneq ($(CACAMOS_APPLIANCE),true)
 ifeq ($(RELEASE_PACKAGE_NFC_STACK),NfcNci)
     PRODUCT_PACKAGES += \
         framework-nfc \
@@ -359,6 +366,7 @@ else
     PRODUCT_PACKAGES += \
         com.android.nfcservices
 endif
+endif
 
 # Check if the build supports Profiling module
 ifeq ($(RELEASE_PACKAGE_PROFILING_MODULE),true)
diff --git a/build/make/target/product/default_art_config.mk b/build/make/target/product/default_art_config.mk
index 0b6f981035..b242253168 100644
--- a/build/make/target/product/default_art_config.mk
+++ b/build/make/target/product/default_art_config.mk
@@ -114,7 +114,13 @@ else
 
 endif
 
-# Check if the build supports NFC apex or not
+# Keep Android's public NFC classes on the platform boot class path even when
+# the appliance omits the NFC service module and hardware feature.
+ifeq ($(CACAMOS_APPLIANCE),true)
+    PRODUCT_BOOT_JARS += \
+        framework-nfc
+endif
+ifneq ($(CACAMOS_APPLIANCE),true)
 ifeq ($(RELEASE_PACKAGE_NFC_STACK),NfcNci)
     PRODUCT_BOOT_JARS += \
         framework-nfc
@@ -123,6 +129,7 @@ else
         com.android.nfcservices:framework-nfc
     $(call soong_config_set,bootclasspath,nfc_apex_bootclasspath_fragment,true)
 endif
+endif
 
 # Check if build supports Profiling module.
 ifeq ($(RELEASE_PACKAGE_PROFILING_MODULE),true)
diff --git a/build/make/target/product/full_base.mk b/build/make/target/product/full_base.mk
index 7aa7903dd4..65f8d75649 100644
--- a/build/make/target/product/full_base.mk
+++ b/build/make/target/product/full_base.mk
@@ -24,10 +24,14 @@ PRODUCT_PACKAGES := \
     WAPPushManager
 
 PRODUCT_PACKAGES += \
-    LiveWallpapersPicker \
-    PhotoTable \
     preinstalled-packages-platform-full-base.xml
 
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
+    LiveWallpapersPicker \
+    PhotoTable
+endif
+
 # Net:
 #   Vendors can use the platform-provided network configuration utilities (ip,
 #   iptable, etc.) to configure the Linux networking stack, but these utilities
diff --git a/build/make/target/product/generic_system.mk b/build/make/target/product/generic_system.mk
index b9a623dcd3..99050c2743 100644
--- a/build/make/target/product/generic_system.mk
+++ b/build/make/target/product/generic_system.mk
@@ -25,8 +25,11 @@ $(call inherit-product-if-exists, vendor/google/security/adb/vendor_key.mk)
 $(call inherit-product, $(SRC_TARGET_DIR)/product/updatable_apex.mk)
 
 # Shared java libs
+ifneq ($(CACAMOS_APPLIANCE),true)
 PRODUCT_PACKAGES += \
     com.android.nfc_extras \
+    Tag
+endif
 
 # Applications
 PRODUCT_PACKAGES += \
@@ -34,7 +37,6 @@ PRODUCT_PACKAGES += \
     PartnerBookmarksProvider \
     preinstalled-packages-platform-generic-system.xml \
     Stk \
-    Tag \
 
 ifeq ($(RELEASE_AVATAR_PICKER_APP),true)
   PRODUCT_PACKAGES += \
diff --git a/build/make/target/product/handheld_product.mk b/build/make/target/product/handheld_product.mk
index b2ce4c5e19..8324db5a83 100644
--- a/build/make/target/product/handheld_product.mk
+++ b/build/make/target/product/handheld_product.mk
@@ -20,7 +20,14 @@
 # does, use base_product.mk).
 $(call inherit-product, $(SRC_TARGET_DIR)/product/media_product.mk)
 
-# /product packages
+# /product packages required by the platform.
+PRODUCT_PACKAGES += \
+    preinstalled-packages-platform-handheld-product.xml \
+    SettingsIntelligence \
+    frameworks-base-overlays
+
+# Consumer applications are not part of a dedicated CaCamOS appliance.
+ifneq ($(CACAMOS_APPLIANCE),true)
 PRODUCT_PACKAGES += \
     Browser2 \
     Calendar \
@@ -28,10 +35,8 @@ PRODUCT_PACKAGES += \
     DeskClock \
     Gallery2 \
     Music \
-    preinstalled-packages-platform-handheld-product.xml \
-    QuickSearchBox \
-    SettingsIntelligence \
-    frameworks-base-overlays
+    QuickSearchBox
+endif
 
 ifeq ($(LINEAGE_BUILD),)
 PRODUCT_PACKAGES += \
diff --git a/build/make/target/product/handheld_system.mk b/build/make/target/product/handheld_system.mk
index 2f8cd4c5dd..47d9222497 100644
--- a/build/make/target/product/handheld_system.mk
+++ b/build/make/target/product/handheld_system.mk
@@ -34,11 +34,8 @@ $(call inherit-product-if-exists, frameworks/webview/chromium/chromium.mk)
 
 PRODUCT_PACKAGES += \
     android.software.window_magnification.prebuilt.xml \
-    BasicDreams \
     BlockedNumberProvider \
-    BluetoothMidiService \
     BookmarkProvider \
-    BuiltInPrintService \
     CalendarProvider \
     cameraserver \
     CameraExtensionsProxy \
@@ -46,33 +43,42 @@ PRODUCT_PACKAGES += \
     CertInstaller \
     CredentialManager \
     DeviceAsWebcam \
-    DeviceDiagnostics \
     DocumentsUI \
-    DownloadProviderUi \
-    EasterEgg \
     ExternalStorageProvider \
     FusedLocation \
     InputDevices \
     KeyChain \
     librs_jni \
-    ManagedProvisioning \
     MmsService \
-    MtpService \
-    MusicFX \
     PacProcessor \
     preinstalled-packages-platform-handheld-system.xml \
-    PrintRecommendationService \
-    PrintSpooler \
     ProxyHandler \
     screenrecord \
     SecureElement \
     SharedStorageBackup \
-    SimAppDialog \
     Telecom \
     TeleService \
     UserDictionaryProvider \
     VpnDialogs \
-    vr \
+    vr
+
+# Consumer and general-purpose utility UIs are omitted from CaCamOS.
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
+    BasicDreams \
+    BluetoothMidiService \
+    BuiltInPrintService \
+    DeviceDiagnostics \
+    DownloadProviderUi \
+    EasterEgg \
+    ManagedProvisioning \
+    MtpService \
+    MusicFX \
+    PrintRecommendationService \
+    PrintSpooler \
+    SimAppDialog \
+    StatementService
+endif
 
 # Choose the correct products based on HSUM status
 ifeq ($(PRODUCT_USE_HSUM),true)
diff --git a/build/make/target/product/handheld_system_ext.mk b/build/make/target/product/handheld_system_ext.mk
index 187b6275bb..f276a16c1a 100644
--- a/build/make/target/product/handheld_system_ext.mk
+++ b/build/make/target/product/handheld_system_ext.mk
@@ -20,12 +20,16 @@
 # does, use base_system_ext.mk).
 $(call inherit-product, $(SRC_TARGET_DIR)/product/media_system_ext.mk)
 
-# /system_ext packages
+# /system_ext packages required by the appliance.
 PRODUCT_PACKAGES += \
-    AccessibilityMenu \
-    Launcher3QuickStep \
-    Provision \
     Settings \
     StorageManager \
     SystemUI \
-    WallpaperCropper \
+    WallpaperCropper
+
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
+    AccessibilityMenu \
+    Launcher3QuickStep \
+    Provision
+endif
diff --git a/build/make/target/product/media_system.mk b/build/make/target/product/media_system.mk
index 0733d06f05..f0022da8c4 100644
--- a/build/make/target/product/media_system.mk
+++ b/build/make/target/product/media_system.mk
@@ -34,8 +34,12 @@ PRODUCT_PACKAGES += \
     libwebviewchromium_loader \
     libwebviewchromium_plat_support \
     make_f2fs \
-    requestsync \
-    StatementService \
+    requestsync
+
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
+    StatementService
+endif
 
 PRODUCT_HOST_PACKAGES += \
     fsck.f2fs \
diff --git a/build/make/target/product/telephony_product.mk b/build/make/target/product/telephony_product.mk
index aa70f46797..3abfa7e6ef 100644
--- a/build/make/target/product/telephony_product.mk
+++ b/build/make/target/product/telephony_product.mk
@@ -17,8 +17,13 @@
 # This is the list of modules that are specific to products that have telephony
 # hardware, and install to the product partition.
 
-# /product packages
+# Keep telephony metadata required by the inherited MI8 device configuration,
+# but omit user-facing phone applications from the webcam appliance.
 PRODUCT_PACKAGES += \
-    Dialer \
-    ImsServiceEntitlement \
     preinstalled-packages-platform-telephony-product.xml
+
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
+    Dialer \
+    ImsServiceEntitlement
+endif
diff --git a/build/make/target/product/telephony_system.mk b/build/make/target/product/telephony_system.mk
index ef48719052..52832a59b5 100644
--- a/build/make/target/product/telephony_system.mk
+++ b/build/make/target/product/telephony_system.mk
@@ -18,10 +18,14 @@
 # hardware, and install on the system partition.
 
 PRODUCT_PACKAGES := \
-    ONS \
+    ONS
+
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
     CarrierDefaultApp \
     CallLogBackup \
     com.android.cellbroadcast \
-    CellBroadcastLegacyApp \
+    CellBroadcastLegacyApp
+endif
 
 PRODUCT_COPY_FILES := \
diff --git a/build/make/target/product/telephony_system_ext.mk b/build/make/target/product/telephony_system_ext.mk
index f821381076..6c38e6dbe1 100644
--- a/build/make/target/product/telephony_system_ext.mk
+++ b/build/make/target/product/telephony_system_ext.mk
@@ -17,10 +17,15 @@
 # This is the list of modules that are specific to products that have telephony
 # hardware, and install to the system_ext partition.
 
-# /system_ext packages
+# CarrierConfig remains available to the inherited radio stack. EmergencyInfo
+# is a user-facing phone application and is omitted from CaCamOS.
 PRODUCT_PACKAGES += \
-    CarrierConfig \
-    EmergencyInfo \
+    CarrierConfig
+
+ifneq ($(CACAMOS_APPLIANCE),true)
+PRODUCT_PACKAGES += \
+    EmergencyInfo
+endif
 
 PRODUCT_PACKAGES_SHIPPING_API_LEVEL_34 += \
     hwservicemanager \

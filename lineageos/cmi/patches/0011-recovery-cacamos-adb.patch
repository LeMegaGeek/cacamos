diff --git a/bootable/recovery/recovery_main.cpp b/bootable/recovery/recovery_main.cpp
index 3fdef4ea..41e68744 100644
--- a/bootable/recovery/recovery_main.cpp
+++ b/bootable/recovery/recovery_main.cpp
@@ -524,6 +524,14 @@ int main(int argc, char** argv) {
   std::thread listener_thread(ListenRecoverySocket, ui, std::ref(action));
   listener_thread.detach();
 
+  if (android::base::GetBoolProperty("ro.cacamos.appliance", false)) {
+    if (!android::base::SetProperty("ro.adb.secure.recovery", "0")) {
+      LOG(ERROR) << "Failed to enable unattended CaCamOS recovery ADB";
+    } else {
+      LOG(INFO) << "CaCamOS recovery ADB enabled for appliance maintenance";
+    }
+  }
+
   // Set up adb_keys and enable root before starting ADB.
   if (get_build_type() != "user" && !fastboot) {
     copy_userdata_files();
diff --git a/bootable/recovery/recovery_utils/roots.cpp b/bootable/recovery/recovery_utils/roots.cpp
index aef1347e..dcdd5179 100644
--- a/bootable/recovery/recovery_utils/roots.cpp
+++ b/bootable/recovery/recovery_utils/roots.cpp
@@ -452,6 +452,11 @@ int setup_install_mounts() {
 
     if (entry.mount_point == "/tmp" || entry.mount_point == "/cache") {
       if (ensure_path_mounted(entry.mount_point) != 0) {
+        if (entry.mount_point == "/cache" &&
+            android::base::GetBoolProperty("ro.cacamos.appliance", false)) {
+          LOG(WARNING) << "CaCamOS: optional /cache is unavailable; continuing installation";
+          continue;
+        }
         LOG(ERROR) << "Failed to mount " << entry.mount_point;
         return -1;
       }

diff --git a/frameworks/base/core/res/AndroidManifest.xml b/frameworks/base/core/res/AndroidManifest.xml
index 769c3d3110df..3b2ea86197e2 100644
--- a/frameworks/base/core/res/AndroidManifest.xml
+++ b/frameworks/base/core/res/AndroidManifest.xml
@@ -26,6 +26,8 @@
     <!-- ================================================ -->
     <eat-comment />
 
+    <protected-broadcast android:name="com.android.DeviceAsWebcam.action.PREPARE_UVC" />
+
     <protected-broadcast android:name="android.intent.action.SCREEN_OFF" />
     <protected-broadcast android:name="android.intent.action.SCREEN_ON" />
     <protected-broadcast android:name="android.intent.action.USER_PRESENT" />
diff --git a/frameworks/base/core/res/res/values/config.xml b/frameworks/base/core/res/res/values/config.xml
index fbe4c3280e49..4d61791fb1a1 100644
--- a/frameworks/base/core/res/res/values/config.xml
+++ b/frameworks/base/core/res/res/values/config.xml
@@ -531,6 +531,12 @@
     <!-- If this is true, the message that USB is only being used for charging will be shown. -->
     <bool name="config_usbChargingMessage">true</bool>
 
+    <!-- If this is true, USB defaults to DeviceAsWebcam/UVC instead of charging/data prompts. -->
+    <bool name="config_usbDefaultToUvc">false</bool>
+
+    <!-- If this is true, secure wireless debugging is enabled at boot and after Wi-Fi reconnects. -->
+    <bool name="config_adbWifiAutoEnable">false</bool>
+
     <!-- Set this true only if the device has separate attention and notification lights. -->
     <bool name="config_useAttentionLight">false</bool>
 
diff --git a/frameworks/base/core/res/res/values/symbols.xml b/frameworks/base/core/res/res/values/symbols.xml
index c3ef357178cb..df9d127cc6bd 100644
--- a/frameworks/base/core/res/res/values/symbols.xml
+++ b/frameworks/base/core/res/res/values/symbols.xml
@@ -2119,6 +2119,8 @@
   <java-symbol type="bool" name="config_sf_limitedAlpha" />
   <java-symbol type="bool" name="config_unplugTurnsOnScreen" />
   <java-symbol type="bool" name="config_usbChargingMessage" />
+  <java-symbol type="bool" name="config_usbDefaultToUvc" />
+  <java-symbol type="bool" name="config_adbWifiAutoEnable" />
   <java-symbol type="bool" name="config_skipScreenOnBrightnessRamp" />
   <java-symbol type="bool" name="config_skipScreenOffTransition" />
   <java-symbol type="bool" name="config_allowAutoBrightnessWhileDozing" />
diff --git a/frameworks/base/services/core/java/com/android/server/adb/AdbDebuggingManager.java b/frameworks/base/services/core/java/com/android/server/adb/AdbDebuggingManager.java
index 1e3229c9e3ab..38a804796814 100644
--- a/frameworks/base/services/core/java/com/android/server/adb/AdbDebuggingManager.java
+++ b/frameworks/base/services/core/java/com/android/server/adb/AdbDebuggingManager.java
@@ -111,6 +111,7 @@ public class AdbDebuggingManager {
 
     private final Context mContext;
     private final ContentResolver mContentResolver;
+    private final boolean mAutoEnableAdbWifi;
     @VisibleForTesting final AdbDebuggingHandler mHandler;
     private boolean mAdbUsbEnabled = false;
     private boolean mAdbWifiEnabled = false;
@@ -121,6 +122,9 @@ public class AdbDebuggingManager {
     @Nullable private final File mUserKeyFile;
     @Nullable private final File mTempKeysFile;
 
+    private static final String WIFI_PERSISTENT_CONFIG_PROPERTY =
+            "persist.adb.tls_server.enable";
+    private static final long ADB_WIFI_RETRY_INTERVAL_MS = 2_000;
     static final String WIFI_PERSISTENT_GUID = "persist.adb.wifi.guid";
     private static final int PAIRING_CODE_LENGTH = 6;
 
@@ -173,6 +177,8 @@ public class AdbDebuggingManager {
             Ticker ticker) {
         mContext = context;
         mContentResolver = mContext.getContentResolver();
+        mAutoEnableAdbWifi = context.getResources().getBoolean(
+                com.android.internal.R.bool.config_adbWifiAutoEnable);
         mConfirmComponent = confirmComponent;
         mUserKeyFile = testUserKeyFile;
         mTempKeysFile = tempKeysFile;
@@ -814,8 +820,16 @@ public class AdbDebuggingManager {
 
                     AdbConnectionInfo currentInfo = getCurrentWifiApInfo();
                     if (currentInfo == null) {
-                        Settings.Global.putInt(
-                                mContentResolver, Settings.Global.ADB_WIFI_ENABLED, 0);
+                        if (mAutoEnableAdbWifi) {
+                            Settings.Global.putInt(mContentResolver,
+                                    Settings.Global.ADB_WIFI_ENABLED, 1);
+                            removeMessages(MSG_ADBDWIFI_ENABLE);
+                            sendEmptyMessageDelayed(
+                                    MSG_ADBDWIFI_ENABLE, ADB_WIFI_RETRY_INTERVAL_MS);
+                        } else {
+                            Settings.Global.putInt(mContentResolver,
+                                    Settings.Global.ADB_WIFI_ENABLED, 0);
+                        }
                         break;
                     }
 
@@ -834,8 +848,8 @@ public class AdbDebuggingManager {
                     startTLSPortPoller();
                     startAdbdWifi();
                     mAdbWifiEnabled = true;
-
-                    Slog.i(TAG, "adb start wireless adb");
+                    SystemProperties.set(WIFI_PERSISTENT_CONFIG_PROPERTY, "1");
+                    Slog.i(TAG, "CaCamOS wireless ADB enabled on " + currentInfo.getSSID());
                 }
                 case MSG_ADBDWIFI_DISABLE -> {
                     if (!mAdbWifiEnabled) {
@@ -869,6 +883,7 @@ public class AdbDebuggingManager {
                     ensureAdbDebuggingThreadAlive();
                     startTLSPortPoller();
                     startAdbdWifi();
+                    SystemProperties.set(WIFI_PERSISTENT_CONFIG_PROPERTY, "1");
                     mAdbWifiEnabled = true;
                     Slog.i(TAG, "adb start wireless adb");
                 }
@@ -944,12 +959,26 @@ public class AdbDebuggingManager {
                 case MSG_SERVER_CONNECTED -> {
                     int port = (int) msg.obj;
                     onAdbdWifiServerConnected(port);
-                    mAdbConnectionInfo.setPort(port);
+                    synchronized (mAdbConnectionInfo) {
+                        mAdbConnectionInfo.setPort(port);
+                    }
+                    Slog.i(TAG, "CaCamOS wireless ADB listening on port " + port);
+                    Settings.Global.putInt(mContentResolver,
+                            Settings.Global.ADB_WIFI_ENABLED, 1);
                 }
                 case MSG_SERVER_DISCONNECTED -> {
                     if (!mAdbWifiEnabled) {
                         break;
                     }
+                    if (mAutoEnableAdbWifi) {
+                        Settings.Global.putInt(mContentResolver,
+                                Settings.Global.ADB_WIFI_ENABLED, 1);
+                        removeMessages(MSG_ADBDWIFI_ENABLE);
+                        sendEmptyMessage(MSG_ADBDWIFI_DISABLE);
+                        sendEmptyMessageDelayed(
+                                MSG_ADBDWIFI_ENABLE, ADB_WIFI_RETRY_INTERVAL_MS);
+                        break;
+                    }
                     int port = (int) msg.obj;
                     onAdbdWifiServerDisconnected(port);
                     stopTLSPortPoller();
@@ -1125,6 +1154,9 @@ public class AdbDebuggingManager {
         }
 
         private boolean verifyWifiNetwork(String bssid, String ssid) {
+            if (mAutoEnableAdbWifi) {
+                return true;
+            }
             // Check against a list of user-trusted networks.
             if (mAdbKeyStore.isTrustedNetwork(bssid)) {
                 return true;
diff --git a/frameworks/base/services/core/java/com/android/server/adb/AdbService.java b/frameworks/base/services/core/java/com/android/server/adb/AdbService.java
index 9dd313f5f1ad..bcf05d0d9e59 100644
--- a/frameworks/base/services/core/java/com/android/server/adb/AdbService.java
+++ b/frameworks/base/services/core/java/com/android/server/adb/AdbService.java
@@ -19,8 +19,11 @@ import static com.android.internal.util.function.pooled.PooledLambda.obtainMessa
 
 import android.annotation.NonNull;
 import android.annotation.UserIdInt;
+import android.content.BroadcastReceiver;
 import android.content.ContentResolver;
 import android.content.Context;
+import android.content.Intent;
+import android.content.IntentFilter;
 import android.content.pm.PackageManager;
 import android.database.ContentObserver;
 import android.debug.AdbManagerInternal;
@@ -31,6 +34,7 @@ import android.debug.IAdbManager;
 import android.debug.IAdbTransport;
 import android.debug.PairDevice;
 import android.hardware.usb.UsbManager;
+import android.net.NetworkInfo;
 import android.net.Uri;
 import android.net.wifi.WifiManager;
 import android.os.Binder;
@@ -210,6 +214,7 @@ public class AdbService extends IAdbManager.Stub {
 
     private final Context mContext;
     private final ContentResolver mContentResolver;
+    private final boolean mAutoEnableAdbWifi;
     private final ArrayMap<IBinder, IAdbTransport> mTransports = new ArrayMap<>();
 
     private boolean mIsAdbUsbEnabled;
@@ -217,16 +222,51 @@ public class AdbService extends IAdbManager.Stub {
     private final AdbDebuggingManager mDebuggingManager;
 
     private ContentObserver mObserver;
+    private final BroadcastReceiver mWifiConnectionReceiver = new BroadcastReceiver() {
+        @Override
+        public void onReceive(Context context, Intent intent) {
+            NetworkInfo networkInfo = intent.getParcelableExtra(
+                    WifiManager.EXTRA_NETWORK_INFO, NetworkInfo.class);
+            if (networkInfo == null || !networkInfo.isConnected()) {
+                return;
+            }
+            try {
+                Settings.Global.putInt(
+                        context.getContentResolver(), Settings.Global.ADB_WIFI_ENABLED, 1);
+            } catch (SecurityException e) {
+                Slog.d(TAG, "Wireless debugging is restricted.");
+                return;
+            }
+            FgThread.getHandler().post(AdbService.this::enableAdbWifiAfterNetworkConnect);
+        }
+    };
 
     private AdbService(Context context) {
         mContext = context;
         mContentResolver = context.getContentResolver();
+        mAutoEnableAdbWifi = context.getResources().getBoolean(
+                com.android.internal.R.bool.config_adbWifiAutoEnable);
         mDebuggingManager = new AdbDebuggingManager(context);
 
         registerContentObservers();
+        if (mAutoEnableAdbWifi) {
+            IntentFilter filter = new IntentFilter(WifiManager.NETWORK_STATE_CHANGED_ACTION);
+            mContext.registerReceiver(mWifiConnectionReceiver, filter);
+        }
         LocalServices.addService(AdbManagerInternal.class, new AdbManagerInternalImpl());
     }
 
+    private void enableAdbWifiAfterNetworkConnect() {
+        if (!mIsAdbWifiEnabled) {
+            setAdbEnabled(true, AdbTransportType.WIFI);
+            return;
+        }
+        startAdbd();
+        if (mDebuggingManager != null) {
+            mDebuggingManager.setAdbEnabled(true, AdbTransportType.WIFI);
+        }
+    }
+
     /**
      * Called in response to {@code SystemService.PHASE_ACTIVITY_MANAGER_READY} from {@code
      * SystemServer}.
@@ -238,15 +278,17 @@ public class AdbService extends IAdbManager.Stub {
          * Use the normal bootmode persistent prop to maintain state of adb across
          * all boot modes.
          */
-        mIsAdbUsbEnabled =
-                containsFunction(
-                        SystemProperties.get(USB_PERSISTENT_CONFIG_PROPERTY, ""),
-                        UsbManager.USB_FUNCTION_ADB);
-        boolean shouldEnableAdbUsb =
-                mIsAdbUsbEnabled
-                        || SystemProperties.getBoolean(
-                                TestHarnessModeService.TEST_HARNESS_MODE_PROPERTY, false);
-        mIsAdbWifiEnabled = "1".equals(SystemProperties.get(WIFI_PERSISTENT_CONFIG_PROPERTY, "0"));
+        mIsAdbUsbEnabled = containsFunction(
+                SystemProperties.get(USB_PERSISTENT_CONFIG_PROPERTY, ""),
+                UsbManager.USB_FUNCTION_ADB);
+        boolean shouldEnableAdbUsb = mIsAdbUsbEnabled
+                || SystemProperties.getBoolean(
+                        TestHarnessModeService.TEST_HARNESS_MODE_PROPERTY, false);
+        mIsAdbWifiEnabled = mAutoEnableAdbWifi || "1".equals(
+                SystemProperties.get(WIFI_PERSISTENT_CONFIG_PROPERTY, "0"));
+        if (mAutoEnableAdbWifi) {
+            SystemProperties.set(WIFI_PERSISTENT_CONFIG_PROPERTY, "1");
+        }
 
         // make sure the ADB_ENABLED setting value matches the current state
         try {
@@ -265,8 +307,13 @@ public class AdbService extends IAdbManager.Stub {
      */
     public void bootCompleted() {
         Slog.d(TAG, "boot completed");
-        mDebuggingManager.setAdbEnabled(mIsAdbUsbEnabled, AdbTransportType.USB);
-        mDebuggingManager.setAdbEnabled(mIsAdbWifiEnabled, AdbTransportType.WIFI);
+        if (mIsAdbWifiEnabled) {
+            startAdbd();
+        }
+        if (mDebuggingManager != null) {
+            mDebuggingManager.setAdbEnabled(mIsAdbUsbEnabled, AdbTransportType.USB);
+            mDebuggingManager.setAdbEnabled(mIsAdbWifiEnabled, AdbTransportType.WIFI);
+        }
     }
 
     @Override
diff --git a/frameworks/base/services/usb/java/com/android/server/usb/UsbDeviceManager.java b/frameworks/base/services/usb/java/com/android/server/usb/UsbDeviceManager.java
index 9a2183128c3f..9373be393f54 100644
--- a/frameworks/base/services/usb/java/com/android/server/usb/UsbDeviceManager.java
+++ b/frameworks/base/services/usb/java/com/android/server/usb/UsbDeviceManager.java
@@ -677,6 +677,7 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
         private final Context mContext;
         private final UsbAlsaManager mUsbAlsaManager;
         private final UsbPermissionManager mPermissionManager;
+        private final boolean mDefaultToUvc;
         private NotificationManager mNotificationManager;
 
         /**
@@ -729,6 +730,8 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
             mUsbAlsaManager = alsaManager;
             mPermissionManager = permissionManager;
             mContentResolver = context.getContentResolver();
+            mDefaultToUvc = context.getResources().getBoolean(
+                    com.android.internal.R.bool.config_usbDefaultToUvc);
 
             mCurrentUser = ActivityManager.getCurrentUser();
             mScreenLocked = true;
@@ -1225,6 +1228,22 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
             setEnabledFunctions(mScreenUnlockedFunctions, false, operationId);
         }
 
+        protected boolean isDefaultToUvcConfigured() {
+            return mDefaultToUvc && UsbManager.isUvcSupportEnabled();
+        }
+
+        protected boolean shouldDefaultToUvc() {
+            return isDefaultToUvcConfigured();
+        }
+
+        protected long getDefaultFunctions() {
+            return shouldDefaultToUvc() ? UsbManager.FUNCTION_UVC : UsbManager.FUNCTION_NONE;
+        }
+
+        private void setDefaultFunctions(int operationId) {
+            setEnabledFunctions(getDefaultFunctions(), false, operationId);
+        }
+
         private static class AdbTransport extends IAdbTransport.Stub {
             private final UsbHandler mHandler;
 
@@ -1246,9 +1265,15 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
          * charging are accounted for.
          */
         long getAppliedFunctions(long functions) {
+            if (isDefaultToUvcConfigured()) {
+                return UsbManager.FUNCTION_UVC;
+            }
             if (functions == UsbManager.FUNCTION_NONE) {
                 return getChargingFunctions();
             }
+            if ((functions & UsbManager.FUNCTION_UVC) != 0) {
+                return UsbManager.FUNCTION_UVC;
+            }
             if (isAdbEnabled()) {
                 return functions | UsbManager.FUNCTION_ADB;
             }
@@ -1285,7 +1310,7 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                                     && mScreenUnlockedFunctions != UsbManager.FUNCTION_NONE) {
                                 setScreenUnlockedFunctions(operationId);
                             } else {
-                                setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                                setDefaultFunctions(operationId);
                             }
                         }
                         updateUsbFunctions();
@@ -1427,7 +1452,7 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                         // If the screen is unlocked, also set current functions.
                         setScreenUnlockedFunctions(operationId);
                     } else {
-                        setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                        setDefaultFunctions(operationId);
                     }
                     break;
                 case MSG_UPDATE_SCREEN_LOCK:
@@ -1444,7 +1469,7 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                     }
                     if (mScreenLocked) {
                         if (!mConnected) {
-                            setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                            setDefaultFunctions(operationId);
                         }
                     } else {
                         if (mScreenUnlockedFunctions != UsbManager.FUNCTION_NONE
@@ -1506,7 +1531,7 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                                     mSettings.getString(String.format(Locale.ENGLISH,
                                             UNLOCKED_CONFIG_PREF, mCurrentUser), ""));
                         }
-                        setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                        setDefaultFunctions(operationId);
                     }
                     break;
                 }
@@ -1579,7 +1604,7 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                         && mScreenUnlockedFunctions != UsbManager.FUNCTION_NONE) {
                     setScreenUnlockedFunctions(operationId);
                 } else {
-                    setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                    setDefaultFunctions(operationId);
                 }
                 if (!Flags.checkUserActionUnlocked()) {
                     attachAccessoryAfterBoot();
@@ -1815,6 +1840,9 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
         }
 
         protected long getChargingFunctions() {
+            if (isDefaultToUvcConfigured()) {
+                return UsbManager.FUNCTION_UVC;
+            }
             // if ADB is enabled, reset functions to ADB
             // else enable MTP as usual.
             if (isAdbEnabled()) {
@@ -1828,6 +1856,19 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
             SystemProperties.set(prop, val);
         }
 
+        protected void prepareDeviceAsWebcamServiceIfNeeded(long functions) {
+            if ((functions & UsbManager.FUNCTION_UVC) == 0) {
+                return;
+            }
+
+            Intent intent = new Intent("com.android.DeviceAsWebcam.action.PREPARE_UVC");
+            intent.setPackage("com.android.DeviceAsWebcam");
+            intent.addFlags(Intent.FLAG_RECEIVER_INCLUDE_BACKGROUND
+                    | Intent.FLAG_RECEIVER_FOREGROUND);
+            mContext.sendBroadcastAsUser(intent, UserHandle.ALL);
+            SystemClock.sleep(750);
+        }
+
         protected String getSystemProperty(String prop, String def) {
             return SystemProperties.get(prop, def);
         }
@@ -2121,6 +2162,9 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
 
         private void setUsbConfig(String config) {
             if (DEBUG) Slog.d(TAG, "setUsbConfig(" + config + ")");
+            if (containsFunction(config, UsbManager.USB_FUNCTION_UVC)) {
+                prepareDeviceAsWebcamServiceIfNeeded(UsbManager.FUNCTION_UVC);
+            }
             /**
              * set the new configuration
              * we always set it due to b/23631400, where adbd was getting killed
@@ -2202,6 +2246,12 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
             if (functions == null) {
                 functions = "";
             }
+            if (isDefaultToUvcConfigured()) {
+                return UsbManager.USB_FUNCTION_UVC;
+            }
+            if (containsFunction(functions, UsbManager.USB_FUNCTION_UVC)) {
+                return UsbManager.USB_FUNCTION_UVC;
+            }
             if (isAdbEnabled()) {
                 functions = addFunction(functions, UsbManager.USB_FUNCTION_ADB);
             } else {
@@ -2458,6 +2508,10 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                     operationId = sUsbOperationCount.incrementAndGet();
                     Slog.e(TAG, "Set functions timed out! no reply from usb hal"
                                 + " ,operationId:" + operationId);
+                    if ((mCurrentFunctions & UsbManager.FUNCTION_UVC) != 0) {
+                        setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                        break;
+                    }
                     if (msg.arg1 != 1) {
                         // Set this since default function may be selected from Developer options
                         setEnabledFunctions(mScreenUnlockedFunctions, false, operationId);
@@ -2483,6 +2537,10 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
                      * Dont force to default when the configuration is already set to default.
                      */
                     operationId = sUsbOperationCount.incrementAndGet();
+                    if ((mCurrentFunctions & UsbManager.FUNCTION_UVC) != 0) {
+                        setEnabledFunctions(UsbManager.FUNCTION_NONE, false, operationId);
+                        break;
+                    }
                     if (msg.arg1 != 1) {
                         if (mCurrentFunctions == UsbManager.FUNCTION_ACCESSORY) {
                             notifyAccessoryModeExit(operationId);
@@ -2600,6 +2658,8 @@ public class UsbDeviceManager implements ActivityTaskManagerInternal.ScreenObser
             removeMessages(MSG_SET_FUNCTIONS_TIMEOUT);
             removeMessages(MSG_SET_CHARGING_FUNCTIONS);
 
+            prepareDeviceAsWebcamServiceIfNeeded(config);
+
             synchronized (mGadgetProxyLock) {
                 if (mUsbGadgetHal == null) {
                     Slog.e(TAG, "setUsbConfig mUsbGadgetHal is null");
diff --git a/frameworks/base/tests/UsbTests/src/com/android/server/usb/UsbHandlerTest.java b/frameworks/base/tests/UsbTests/src/com/android/server/usb/UsbHandlerTest.java
index c2d0f7c05b91..c9277b7e0321 100644
--- a/frameworks/base/tests/UsbTests/src/com/android/server/usb/UsbHandlerTest.java
+++ b/frameworks/base/tests/UsbTests/src/com/android/server/usb/UsbHandlerTest.java
@@ -96,6 +96,7 @@ public class UsbHandlerTest {
 
     private class MockUsbHandler extends UsbDeviceManager.UsbHandler {
         boolean mIsUsbTransferAllowed;
+        boolean mDefaultToUvc;
         Intent mBroadcastedIntent;
 
         MockUsbHandler(Looper looper, Context context, UsbDeviceManager deviceManager,
@@ -135,6 +136,11 @@ public class UsbHandlerTest {
             return mIsUsbTransferAllowed;
         }
 
+        @Override
+        protected boolean isDefaultToUvcConfigured() {
+            return mDefaultToUvc;
+        }
+
         @Override
         protected SharedPreferences getPinnedSharedPrefs(Context context) {
             return mSharedPreferences;
@@ -248,6 +254,42 @@ public class UsbHandlerTest {
         assertEquals(UsbManager.FUNCTION_NCM, mUsbHandler.getEnabledFunctions() & rndisPlusNcm);
     }
 
+    @SmallTest
+    @Test
+    public void getAppliedFunctions_addsAdbToDataFunctions() {
+        when(mAdbManagerInternal.isAdbEnabled(eq(AdbTransportType.USB))).thenReturn(true);
+
+        assertEquals(UsbManager.FUNCTION_MTP | UsbManager.FUNCTION_ADB,
+                mUsbHandler.getAppliedFunctions(UsbManager.FUNCTION_MTP));
+    }
+
+    @SmallTest
+    @Test
+    public void getAppliedFunctions_keepsUvcWithoutAdb() {
+        when(mAdbManagerInternal.isAdbEnabled(eq(AdbTransportType.USB))).thenReturn(true);
+
+        assertEquals(UsbManager.FUNCTION_UVC,
+                mUsbHandler.getAppliedFunctions(UsbManager.FUNCTION_UVC));
+        assertEquals(UsbManager.FUNCTION_UVC,
+                mUsbHandler.getAppliedFunctions(
+                        UsbManager.FUNCTION_UVC | UsbManager.FUNCTION_ADB));
+    }
+
+    @SmallTest
+    @Test
+    public void getAppliedFunctions_cacamOsAlwaysUsesUvcAlone() {
+        mUsbHandler.mDefaultToUvc = true;
+        when(mAdbManagerInternal.isAdbEnabled(eq(AdbTransportType.USB))).thenReturn(true);
+
+        assertEquals(UsbManager.FUNCTION_UVC,
+                mUsbHandler.getAppliedFunctions(UsbManager.FUNCTION_NONE));
+        assertEquals(UsbManager.FUNCTION_UVC,
+                mUsbHandler.getAppliedFunctions(UsbManager.FUNCTION_MTP));
+        assertEquals(UsbManager.FUNCTION_UVC,
+                mUsbHandler.getAppliedFunctions(
+                        UsbManager.FUNCTION_RNDIS | UsbManager.FUNCTION_ADB));
+    }
+
     @SmallTest
     @Test
     public void enableAdb() {
@@ -293,6 +335,16 @@ public class UsbHandlerTest {
         assertEquals(mUsbHandler.getEnabledFunctions(), UsbManager.FUNCTION_NONE);
     }
 
+    @SmallTest
+    @Test
+    public void bootCompletedDefaultsToUvcBeforeUserUnlock() {
+        mUsbHandler.mDefaultToUvc = true;
+
+        sendBootCompleteMessages(mUsbHandler);
+
+        assertEquals(UsbManager.FUNCTION_UVC, mUsbHandler.getEnabledFunctions());
+    }
+
     @SmallTest
     @Test
     public void userSwitchedDisablesMtp() {
@@ -332,6 +384,21 @@ public class UsbHandlerTest {
         assertEquals(mUsbHandler.getEnabledFunctions(), UsbManager.FUNCTION_NONE);
     }
 
+    @SmallTest
+    @Test
+    public void disconnectResetsToUvcWhenConfigured() {
+        mUsbHandler.mDefaultToUvc = true;
+        sendBootCompleteMessages(mUsbHandler);
+
+        mUsbHandler.handleMessage(mUsbHandler.obtainMessage(MSG_SET_CURRENT_FUNCTIONS,
+                UsbManager.FUNCTION_MTP));
+        assertNotEquals(mUsbHandler.getEnabledFunctions() & UsbManager.FUNCTION_MTP, 0);
+
+        mUsbHandler.handleMessage(mUsbHandler.obtainMessage(MSG_UPDATE_STATE, 0, 0));
+
+        assertEquals(UsbManager.FUNCTION_UVC, mUsbHandler.getEnabledFunctions());
+    }
+
     @SmallTest
     @Test
     public void configuredSendsBroadcast() {
